import{_ as t,o,c as n,a3 as a,aw as s}from"./chunks/framework.0RzNNOGV.js";const g=JSON.parse('{"title":"Components","description":"","frontmatter":{},"headers":[],"relativePath":"pages/components.md","filePath":"pages/components.md","lastUpdated":1781638381000}'),r={name:"pages/components.md"};function i(p,e,l,h,c,d){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="components" tabindex="-1">Components <a class="header-anchor" href="#components" aria-label="Permalink to “Components”">​</a></h1><p>Components are a Glyphr Studio feature that lets you re-use a collection of paths across many different glyphs. The root is called a &#39;Component&#39; and these are added by reference to other glyphs, where they are called &#39;Component Instances&#39;. Updating the root component will also update all component instances.</p><p><img src="'+s+'" alt="Components page"></p><p>Components are a type of <code>Glyph</code> that is not directly associated with a character or letter in your font. Components lets you define and re-use a collection of paths across many different glyphs. The root is called a &#39;Component&#39; and these are added by reference to other glyphs, where they are called &#39;Component Instances&#39;. Updating the root component will also update all component instances.</p><p>Diacritic glyphs (glyphs with accents) are just one example of where having a shared component root can be used across many individual characters.</p><p>Glyphr Studio Components (the roots) are not exported to fonts - they are a concept that is only in place to help design a font. All Component Instances will be exported to your font, along with regular Paths. Depending on your export settings and target format, Component Instances are exported either as TrueType composite glyphs (referencing the component&#39;s outlines) or as standard flattened font outlines. Composite glyphs can also be imported back as Components - see <a href="#components-composite-glyphs-round-tripping">Components &amp; composite glyphs round-tripping</a> below.</p><h2 id="component-roots-and-component-instances" tabindex="-1">Component Roots and Component Instances <a class="header-anchor" href="#component-roots-and-component-instances" aria-label="Permalink to “Component Roots and Component Instances”">​</a></h2><p>The link between roots and instances is shown on both glyphs, making it easy to navigate back and forth.</p><p>Any Glyph object (Character, Ligature, or Component) can be used as a component root. When you are on the edit page for a glyph that is being used as a root component, a card with a list of links will be shown in the panel area. Clicking on one of these links will navigate you to the other glyph that is using the root as a Component Instance.</p><p>Component Instances are shown along-side Paths - in both the Layers panel, and as a Component Instance card that is similar to the Path card in the Attributes panel. There will also be a link when a Component Instance is selected that will navigate you back to the Root Component glyph.</p><p>In the Layers Panel as well as on the Edit Canvas, regular paths have a 🟦Blue accent color. Component Instances have a 🟩Green accent color.</p><h2 id="component-instance-transforms" tabindex="-1">Component Instance transforms <a class="header-anchor" href="#component-instance-transforms" aria-label="Permalink to “Component Instance transforms”">​</a></h2><p>Many times, you&#39;ll only need to use the Component Instance with no changes with regards to it&#39;s Component Root. But, you may have to move, resize, or otherwise adjust a Component Instance to make it work in the destination glyph.</p><p>Component Instances <em>do not</em> have size or position attributes. Everything is in relation to it&#39;s Root Component. Component Instances only have x and y offsets, changes in width and height, and other transform information. This way, when the Root Component is edited, a Component Instance inherits all the changes, and then applies any additional transforms as needed.</p><h3 id="available-transforms" tabindex="-1">Available transforms <a class="header-anchor" href="#available-transforms" aria-label="Permalink to “Available transforms”">​</a></h3><table tabindex="0"><thead><tr><th>Transform name</th><th>Description</th></tr></thead><tbody><tr><td><strong>Δ x</strong> and <strong>Δ y</strong></td><td>The difference in x or y position, as compared to the Root Component.</td></tr><tr><td><strong>Δ width</strong> and <strong>Δ height</strong></td><td>The difference in width or height, as compared to the Root Component.</td></tr><tr><td><strong>flip vertical</strong></td><td>Flip top to bottom, as compared to the Root Component.</td></tr><tr><td><strong>flip horizontal</strong></td><td>Flip left to right, as compared to the Root Component.</td></tr><tr><td><strong>reverse winding</strong></td><td>Reverse all the windings, as compared to the paths in the Root Component.</td></tr><tr><td><strong>rotation</strong></td><td>Number of degrees of rotation* as compared to the Root Component.</td></tr><tr><td><strong>rotate first</strong></td><td>When applying both rotation and size changes to a shape, the order of rotate/resize or resize/rotate will end up with two different results. Glyphr Studio tries to be smart about which to do first, but selecting this option will force the rotate transform to happen before the resize transform.</td></tr></tbody></table><p><strong>*Rotation Note</strong> Glyph Studio uses an angle system of Degrees - 360° total, with the top being 0°, moving clockwise for positive values and counter-clockwise for negative values. The bottom of the circle (6 o&#39;clock or 180°) the values switch between positive and negative. So, rotating a shape counter-clockwise will use angle values -1° through -179°, and rotating a shape clockwise will use angle values 0° through 180°.</p><h2 id="components-composite-glyphs-round-tripping" tabindex="-1">Components &amp; composite glyphs round-tripping <a class="header-anchor" href="#components-composite-glyphs-round-tripping" aria-label="Permalink to “Components &amp; composite glyphs round-tripping”">​</a></h2><p>Many fonts already use TrueType <em>composite glyphs</em> - characters built by referencing another glyph&#39;s outlines at an x/y offset, rather than storing their own outlines. Accented letters like <code>à á â</code> and the dotted <code>i</code> and <code>j</code> are common examples. Glyphr Studio&#39;s Components map naturally onto this concept, so it can import and export composites while keeping your Components intact.</p><p>Two paired settings on the <a href="./settings.html#project">Project settings</a> page control this, and both are on by default:</p><ul><li><strong>Import composite glyphs as components</strong> - when opening or importing a font, composite glyphs are brought in as Components and Component Instances instead of being flattened.</li><li><strong>Export components as composite glyphs</strong> - when exporting, characters built from Components are written back out as composite glyphs instead of being flattened.</li></ul><p>With both on (the default), importing a font and re-exporting it as <code>.ttf</code>, <code>.woff</code>, or <code>.woff2</code> keeps Components intact end-to-end - accented characters and other composites stay editable as linked Components rather than collapsing into standalone outlines.</p><h3 id="what-qualifies" tabindex="-1">What qualifies <a class="header-anchor" href="#what-qualifies" aria-label="Permalink to “What qualifies”">​</a></h3><p>The same core rules apply on both import and export:</p><ul><li><strong>Pure-position only.</strong> A component qualifies only when it is a plain translation (an x/y offset). On import, composite components that are scaled, rotated, or use point-matching (anchor attachment) are flattened to outlines. On export, Component Instances that have any resize (Δ width / Δ height), rotation, horizontal/vertical flip, or reversed winding are flattened. A single glyph can be partially composite and partially flattened, on a per-component basis.</li><li><strong>Shared roots are re-used.</strong> On import, a Component Root is created once per referenced base glyph and then linked by every character that uses it. For example, the <code>a</code> outline becomes a single Component shared by <code>à á â ã ä å ā ă ą</code>, and the combining dot becomes one Component shared by <code>i</code>, <code>j</code>, and the dotted accents. The composite&#39;s per-character offset is preserved as the Component Instance&#39;s translate values.</li><li><strong>TrueType only / the <code>.otf</code> caveat.</strong> Composite glyphs only exist in TrueType-flavored formats (<code>.ttf</code>, <code>.woff</code>, <code>.woff2</code>). OpenType/CFF (<code>.otf</code>) cannot store composites, so on export to <code>.otf</code> every component is always flattened to outlines regardless of the setting. Same-format round-trips (TTF → Glyphr Studio → TTF) preserve component structure with high fidelity; when the format changes (for example TTF → OTF), only the conceptual/visual result is preserved, not the composite structure.</li><li><strong>Turning either off.</strong> Unchecking <em>Import composite glyphs as components</em> flattens composites to plain outlines on import (the previous behavior). Unchecking <em>Export components as composite glyphs</em> produces fully flattened outlines for every glyph on export - useful for maximum compatibility with tools that don&#39;t handle composite glyphs well.</li></ul>',25)])])}const f=t(r,[["render",i]]);export{g as __pageData,f as default};
